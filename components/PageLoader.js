import React from "react";
// import Spinner from "../public/img/load-image.gif";

const PageLoader = () => {
  return (
    <div className="loader">
      <img src="/img/load-image.gif" className="sp-loader" alt="loading" />
    </div>
  );
};

export default PageLoader;
